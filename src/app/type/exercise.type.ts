

export type ExerciseType = {
    id : string,
    lastTrain : string
    name: string,
    weight: string,
    repeats: string
    comment: string,
}

export type ExreciseNameIdType = {
    name: string,
    id: string
}